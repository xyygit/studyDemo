<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.fn.demo">

    <application
        android:name=".application.BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".base.BaseActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name=".ui.FirstActivity"
            android:launchMode="standard" />
        <activity
            android:name=".ui.SecondActivity"
            android:launchMode="singleInstance" />
        <activity android:name=".ui.ThirdActivity" />
        <activity android:name=".test.PercentLayoutActivity"></activity>
        <activity android:name=".test.DataBaseActivity"></activity>
        <activity android:name=".test.BroadCastActivity"></activity>
    </application>

</manifest>